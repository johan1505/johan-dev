{
	"name": "infra",
	"version": "0.0.0",
	"private": true,
	"bin": {
		"infra": "bin/infra.js"
	},
	"scripts": {
		"build": "tsc && cdk synth",
		"watch": "tsc -w",
		"cdk": "cdk",
		"deploy": "npm run test && npm run build && cdk deploy --all --require-approval never",
		"dev:fix": "biome lint --write . && biome format --write .",
		"test:format": "biome format .",
		"test:type-check": "tsc --noEmit",
		"test": "npm-run-all test:*"
	},
	"devDependencies": {
		"@aws-sdk/client-ses": "^3.982.0",
		"@aws-sdk/client-sqs": "^3.982.0",
		"@biomejs/biome": "latest",
		"@types/aws-lambda": "latest",
		"@types/node": "latest",
		"aws-cdk": "latest",
		"dotenv": "latest",
		"npm-run-all": "latest",
		"ts-node": "latest",
		"typescript": "latest"
	},
	"dependencies": {
		"@johan-dev/types": "*",
		"aws-cdk-lib": "latest",
		"constructs": "latest",
		"esbuild": "^0.27.3",
		"source-map-support": "latest"
	}
}
